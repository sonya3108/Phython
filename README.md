# Phython

-python -m venv venv
-win -- .\venv\Scripts\aсtivate
-pip or pip3

-pip list
-pip install pywebio
-pip freeze > requirements.txt pip3 install -r requirements.txt auto code formatting

Ctrl - Alt - l
Ctrl - Shift - f - search everywhere
pip install requests

pip3 freeze > requirements.txt 
pip3 install -r requirements.txt


pytest .
pytest -v  .
pytest -v -s  . 
pytest -v  test_utils.py::TestAddTwoNumbers::test_add_two_combined 
pytest -v  test_utils.py::TestAddTwoNumbers

